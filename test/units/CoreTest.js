function CoreTest (require) {
    var ok = require('ok'),
        equal = require('equal'),
        test = require('test'),
        expect = require('expect'),
        module = require('module');

    module('Core');

    test("Core test", function() {
        ok(false, "implement");
    });
}